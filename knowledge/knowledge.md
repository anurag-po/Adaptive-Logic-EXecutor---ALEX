# SYSTEM ACTIONS
- open_app(app_name)
- close_app(app_name)
- shutdown_system()
- restart_system()

# FILE ACTIONS
- create_folder(path, name)
- open_folder(path)
- search_file(query)
- delete_file(path)
- move_file(source, destination)
- open_recent_files(limit)

# SCREEN CONTROL
- screenshot_desktop(save_path, delay)

# BROWSER ACTIONS
- open_website(url)
- search_google(query)
- open_youtube_video(query)
- play_youtube_music(query)

# BROWSER AUTOMATION (Phase 2)
- browse_and_extract(url, selector)
- fill_web_form(url, fields)
- take_page_screenshot(url, save_path)
- click_element(url, selector)

# PRODUCTIVITY
- write_document(topic, length, tone)
- summarize_text(text)
- generate_email(to, subject, body)

# UTILITIES
- calculate(expression)
- set_timer(seconds)
- set_alarm(time, label)

# MEDIA
- play_music(song_name)
- pause_media()
- resume_media()

# MEMORY
- recall_last_action()
- set_preference(key, value)

# BACKGROUND AGENT (Phase 3)
- schedule_reminder(message, delay_minutes)
- list_scheduled_tasks()
- cancel_scheduled_task(task_id)
